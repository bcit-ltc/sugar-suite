services:
  sugar-suite-v1:
    image: sugar-suite-v1
    build:
      context: .
      target: builder

    command: npm run watch

    environment:
      - DEBUG=True

    working_dir: /app

    volumes:
      - .:/app
      - /app/node_modules

    ports:
      - 8080:80
