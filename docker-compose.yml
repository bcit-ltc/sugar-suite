name: sugar-suite

services:
  app:
    image: sugar-suite
    build:
      target: builder
    command: npm run watch
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "8080:80"
    environment:
      - DEBUG=true
      - CI=false
